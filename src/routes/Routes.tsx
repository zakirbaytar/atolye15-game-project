import React, { FunctionComponent } from 'react';
import { Routes, Route } from 'react-router-dom';

const AppRoutes: FunctionComponent = () => <Routes>{/** TODO: Add routes here */}</Routes>;

export default AppRoutes;
